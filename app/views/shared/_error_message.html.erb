<% if object.errors.any? %>
	<h4>Please correct the <%= pluralize(object.errors.count, "error") %> to save or update record.</h4>
	<% object.errors.full_messages.each do |msg| %>
		<ul>
			<li><%= msg %></li>
		</ul>
	<% end %>
<% end %>
